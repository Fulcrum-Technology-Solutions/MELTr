{
  "name": "MELTr",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "meltr-api",
      "description": "MELTr FastAPI management plane + engine on http://127.0.0.1:8080 (foreground; logs visible here).",
      "command": "source .venv/bin/activate && export MELTR_HOME=$HOME/.meltr && meltr init 2>/dev/null; meltr start --foreground"
    }
  ]
}
