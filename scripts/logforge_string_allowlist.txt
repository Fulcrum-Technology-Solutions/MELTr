# Hard cut (2026-08-30): no LogForge product/compat strings in scanned paths.
# Historical notes live under docs/superpowers/ (excluded from the gate).
#
# Allowed: tests that assert the pipelines feature was removed (not product copy).
"pipelines" not in
not expose a pipelines field
hasattr(cfg, "pipelines")
